
import 'package:flutter/material.dart';

class AllIcons{
  static const selectIcon = Icon(Icons.select_all);
  static const searchIcon = Icon(Icons.search);
  static const settingsIcon = Icon(Icons.settings);
  static const noteIcon = Icon(Icons.note);
  static const favoritIcon = Icon(Icons.favorite);
  static const addIcon = Icon(Icons.add);
}